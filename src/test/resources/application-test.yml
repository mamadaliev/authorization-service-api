server:
  port: 8065

spring:
  profiles:
    active: test
  data:
    mongodb:
      uri: mongodb://localhost:27017/auth-db-test

security:
  token:
    secret: "secret"
    expire: 3600000
    type: "bearer"

dozer:
  cache:
    converter-by-dest-type-maxsize: 5000
    super-type-maxsize: 5000
